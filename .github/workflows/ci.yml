name: CI
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: JavaScript syntax
        run: node --check web/app.js && node --check web/runtime.js && node --check web/supabase-local.js && node --check web/verification-gate.js && node --check web/sw.js && node --check tests/smoke.mjs
      - name: Smoke checks
        run: node tests/smoke.mjs
