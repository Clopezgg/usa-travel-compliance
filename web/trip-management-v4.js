import { createClient } from './supabase-local.js';
import { SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY } from './config.js';
const supabase=createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const $=id=>document.getElementById(id);let editingTripId=null;
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const fmtDate=v=>v?new Date(`${v}T12:00:00`).toLocaleDateString('es-HN',{day:'numeric',month:'short',year:'numeric'}).replace('.',''):'Pendiente';
const icon=(name,cls='')=>`<svg class="icon ${cls}" aria-hidden="true"><use href="#i-${name}"></use></svg>`;
function toast(message,type=''){const t=$('toast');if(!t)return;t.textContent=message;t.className=`toast ${type}`;clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.add('hidden'),4200);}
function fail(error){console.error(error);toast(error?.message||String(error)||'No se pudo completar la operación.','error');}
function closeDialog(id){const d=$(id);if(d?.open)d.close();}function openDialog(id){const d=$(id);if(d&&!d.open)d.showModal();}
function ensureStyles(){if(document.getElementById('entrysafe-trip-manager-v4-style'))return;const s=document.createElement('style');s.id='entrysafe-trip-manager-v4-style';s.textContent=`.trip-manager-row{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:12px;padding:11px;border:1px solid #dfe5eb;border-radius:16px;background:#fff}.trip-manager-main{appearance:none;border:0;background:transparent;padding:2px;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:12px;text-align:left;min-width:0;cursor:pointer;color:inherit}.trip-manager-main>div{min-width:0}.trip-manager-main b,.trip-manager-main small{display:block}.trip-manager-main small{margin-top:3px;color:#6f7e90;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.trip-manager-actions{display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end}.trip-action{appearance:none;border:1px solid #dfe5eb;background:#fff;border-radius:10px;padding:8px 9px;font:inherit;font-size:11px;font-weight:780;cursor:pointer}.trip-action:hover{background:#f5f7f8}.trip-action.delete{color:#b42318;border-color:#f0cbc7}.trip-action.warn{color:#94620f}.trip-status{font-size:10px;font-weight:800;border-radius:99px;padding:6px 8px;background:#eff2f5;color:#667789}.trip-status.active{background:#e9f7f1;color:#107a55}.trip-status.cancelled{background:#fff0ee;color:#b42318}.trip-status.completed{background:#edf4ff;color:#195ec8}@media(max-width:650px){.trip-manager-row{grid-template-columns:1fr}.trip-manager-actions{justify-content:flex-start}.trip-manager-main{grid-template-columns:auto minmax(0,1fr) auto}}`;document.head.appendChild(s);}
async function currentUser(){const s=await supabase.auth.getSession();return {session:s.data.session,user:s.data.session?.user||null};}
async function fetchTrips(){const {user}=await currentUser();if(!user)throw new Error('Tu sesión expiró.');const q=await supabase.from('trips').select('*').eq('user_id',user.id).order('departure_date',{ascending:false});if(q.error)throw q.error;return q.data||[];}
function selectedTripId(trips){const remembered=localStorage.getItem('entrysafe-v3-trip')||localStorage.getItem('entrysafe-v4-trip');if(trips.some(t=>t.id===remembered))return remembered;const today=new Date().toISOString().slice(0,10);return [...trips].filter(t=>!['cancelled','completed'].includes(t.status)&&t.departure_date>=today).sort((a,b)=>String(a.departure_date).localeCompare(String(b.departure_date)))[0]?.id||trips.find(t=>t.status==='active')?.id||trips[0]?.id||null;}
function statusLabel(status,active){if(active)return ['Activo','active'];if(status==='completed')return ['Cerrado','completed'];if(status==='cancelled')return ['Cancelado','cancelled'];return ['Planificado',''];}
function setTripFormMode(trip=null){editingTripId=trip?.id||null;const form=$('tripForm');if(!form)return;form.reset();const eyebrow=$('tripDialog')?.querySelector('.dialog-head .overline');const title=$('tripDialog')?.querySelector('.dialog-head h2');const submit=form.querySelector('button[type="submit"]');if(trip){if(eyebrow)eyebrow.textContent='Editar viaje';if(title)title.textContent=`Honduras → ${trip.entry_airport||'Estados Unidos'}`;if(submit)submit.textContent='Guardar cambios';$('tripDeparture').value=trip.departure_date||'';$('tripReturn').value=trip.return_date||'';$('tripAirport').value=trip.entry_airport||'';$('tripState').value=trip.entry_state||'';$('tripAirline').value=trip.airline||'';$('tripFlight').value=trip.flight_number||'';$('tripPurpose').value=trip.purpose||'tourism';}else{if(eyebrow)eyebrow.textContent='Nuevo viaje';if(title)title.textContent='Honduras → Estados Unidos';if(submit)submit.textContent='Crear viaje';$('tripDeparture').value=new Date(Date.now()+7*86400000).toISOString().slice(0,10);}}
async function selectTrip(id){localStorage.setItem('entrysafe-v3-trip',id);localStorage.setItem('entrysafe-v4-trip',id);closeDialog('tripPicker');location.reload();}
async function editTrip(id){try{const trip=(await fetchTrips()).find(t=>t.id===id);if(!trip)throw new Error('Ese viaje ya no existe.');closeDialog('tripPicker');setTripFormMode(trip);openDialog('tripDialog');}catch(error){fail(error);}}
async function setStatus(id,status){try{const trips=await fetchTrips(),trip=trips.find(t=>t.id===id);if(!trip)throw new Error('Ese viaje ya no existe.');const verb=status==='completed'?'cerrar':status==='cancelled'?'cancelar':'reabrir';if(status!=='planned'&&!confirm(`¿Quieres ${verb} el viaje SAP → ${trip.entry_airport||'USA'}?\n\nEl expediente y sus artículos se conservarán.`))return;const {user}=await currentUser();const q=await supabase.from('trips').update({status}).eq('id',id).eq('user_id',user.id);if(q.error)throw q.error;toast(status==='completed'?'Viaje cerrado.':status==='cancelled'?'Viaje cancelado.':'Viaje reabierto.','success');setTimeout(()=>location.reload(),180);}catch(error){fail(error);}}
async function removeStoredDocuments(tripId,userId,session){try{const q=await supabase.from('trip_documents').select('storage_path').eq('trip_id',tripId).eq('user_id',userId);if(q.error)throw q.error;const prefixes=(q.data||[]).map(x=>x.storage_path).filter(Boolean);if(!prefixes.length)return;const response=await fetch(`${SUPABASE_URL}/storage/v1/object/travel-documents`,{method:'DELETE',headers:{apikey:SUPABASE_PUBLISHABLE_KEY,Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({prefixes}),cache:'no-store'});if(!response.ok)console.warn('Document cleanup warning',response.status);}catch(error){console.warn('Document cleanup warning',error);}}
async function deleteTrip(id){try{const trips=await fetchTrips(),trip=trips.find(t=>t.id===id);if(!trip)throw new Error('Ese viaje ya no existe.');if(!confirm(`¿Eliminar definitivamente el viaje SAP → ${trip.entry_airport||'USA'} del ${fmtDate(trip.departure_date)}?\n\nSe eliminarán checklist, maletas, documentos y verificaciones. Esta acción no se puede deshacer.`))return;const {session,user}=await currentUser();if(!session||!user)throw new Error('Tu sesión expiró.');await removeStoredDocuments(id,user.id,session);const q=await supabase.from('trips').delete().eq('id',id).eq('user_id',user.id);if(q.error)throw q.error;if(localStorage.getItem('entrysafe-v3-trip')===id)localStorage.removeItem('entrysafe-v3-trip');if(localStorage.getItem('entrysafe-v4-trip')===id)localStorage.removeItem('entrysafe-v4-trip');toast('Viaje eliminado.','success');setTimeout(()=>location.reload(),180);}catch(error){fail(error);}}
async function renderTripPicker(){try{ensureStyles();const trips=await fetchTrips();if(!trips.length){setTripFormMode(null);openDialog('tripDialog');return;}const activeId=selectedTripId(trips),host=$('tripList');host.innerHTML=trips.map(t=>{const active=activeId===t.id,[status,statusClass]=statusLabel(t.status,active);const lifecycle=['completed','cancelled'].includes(t.status)?`<button class="trip-action" type="button" data-trip-reopen="${t.id}">Reabrir</button>`:`<button class="trip-action" type="button" data-trip-close="${t.id}">Cerrar</button><button class="trip-action warn" type="button" data-trip-cancel="${t.id}">Cancelar</button>`;return `<div class="trip-manager-row"><button class="trip-manager-main" type="button" data-trip-select="${t.id}"><span class="item-icon">${icon('plane')}</span><div><b>${esc(`SAP → ${t.entry_airport||'USA'}`)}</b><small>${fmtDate(t.departure_date)} · ${esc(t.airline||'Aerolínea pendiente')}</small></div><span class="trip-status ${statusClass}">${status}</span></button><div class="trip-manager-actions"><button class="trip-action" type="button" data-trip-edit="${t.id}">Editar</button>${lifecycle}<button class="trip-action delete" type="button" data-trip-delete="${t.id}">Eliminar</button></div></div>`;}).join('');host.querySelectorAll('[data-trip-select]').forEach(b=>b.addEventListener('click',()=>selectTrip(b.dataset.tripSelect)));host.querySelectorAll('[data-trip-edit]').forEach(b=>b.addEventListener('click',()=>editTrip(b.dataset.tripEdit)));host.querySelectorAll('[data-trip-close]').forEach(b=>b.addEventListener('click',()=>setStatus(b.dataset.tripClose,'completed')));host.querySelectorAll('[data-trip-cancel]').forEach(b=>b.addEventListener('click',()=>setStatus(b.dataset.tripCancel,'cancelled')));host.querySelectorAll('[data-trip-reopen]').forEach(b=>b.addEventListener('click',()=>setStatus(b.dataset.tripReopen,'planned')));host.querySelectorAll('[data-trip-delete]').forEach(b=>b.addEventListener('click',()=>deleteTrip(b.dataset.tripDelete)));openDialog('tripPicker');}catch(error){fail(error);}}
async function saveEditedTrip(event){if(!editingTripId)return;event.preventDefault();event.stopImmediatePropagation();const departure=$('tripDeparture').value,returnDate=$('tripReturn').value||null;if(returnDate&&returnDate<departure)return toast('El regreso no puede ser anterior a la salida.','error');const {user}=await currentUser();if(!user)return fail(new Error('Tu sesión expiró.'));const submit=$('tripForm').querySelector('button[type="submit"]'),old=submit.textContent;submit.disabled=true;submit.textContent='Guardando…';try{const payload={departure_date:departure,return_date:returnDate,entry_airport:$('tripAirport').value.trim().toUpperCase(),entry_state:$('tripState').value.trim().toUpperCase()||null,airline:$('tripAirline').value.trim()||null,flight_number:$('tripFlight').value.trim()||null,purpose:$('tripPurpose').value};const q=await supabase.from('trips').update(payload).eq('id',editingTripId).eq('user_id',user.id);if(q.error)throw q.error;localStorage.setItem('entrysafe-v3-trip',editingTripId);localStorage.setItem('entrysafe-v4-trip',editingTripId);toast('Viaje actualizado.','success');closeDialog('tripDialog');editingTripId=null;setTimeout(()=>location.reload(),180);}catch(error){fail(error);submit.disabled=false;submit.textContent=old;}}
function bind(){ensureStyles();$('tripSwitcher')?.addEventListener('click',event=>{event.preventDefault();event.stopImmediatePropagation();renderTripPicker();},true);$('tripForm')?.addEventListener('submit',saveEditedTrip,true);$('newTripPickerBtn')?.addEventListener('click',event=>{event.preventDefault();event.stopImmediatePropagation();setTripFormMode(null);closeDialog('tripPicker');openDialog('tripDialog');},true);document.addEventListener('click',event=>{if(event.target.closest('[data-action="new-trip"]'))setTripFormMode(null);},true);$('tripDialog')?.addEventListener('close',()=>{editingTripId=null;});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
